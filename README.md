# Foyer

A context facade for MCP tools. Users curate task-specific tools that internally compose calls to upstream APIs (Linear, Notion, Craft, etc.). Each Foyer MCP gets a dedicated MCP URL.

## Development

Run `bun install` to install dependencies.
Run `bun run format` to format the codebase.
